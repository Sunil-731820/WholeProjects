import { Display,javaT } from "./Mobile";

// Creating the object of that Class
const d = new Display();
// calling the function within the same class
d.show();

// calling the function of outside the class
javaT();
